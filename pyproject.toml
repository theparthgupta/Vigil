[tool.pytest.ini_options]
testpaths = ["tests"]
# rootdir is the project root; pytest adds it to sys.path automatically

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
